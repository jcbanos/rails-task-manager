<h1>All tasks</h1>

<ul>
  <% @tasks.each do |task|%>
  <li><%= link_to task.title, task %>  <%= link_to "Delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
  <% end %></li>

</ul>

<p><%=link_to "Add task", new_path %></p>
